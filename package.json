{
  "name": "avian-annotator",
  "version": "1.0.0",
  "scripts": {
    "start": "bun install --cwd tui && bun run tui/App.js",
    "build": "docker-compose build",
    "prepare": "husky install",
    "setup": "bun install && cd backend && mvn install -DskipTests"
  },
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@ngx-devs/commitlint-plugin-imperative": "^1.1.4",
    "@types/bun": "latest",
    "husky": "^8.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
